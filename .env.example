# Frontend URL pointing to Railway backend (set after deploying backend)
NEXT_PUBLIC_API_URL=https://your-backend.railway.app

# Internal server-side API URL (same as above for Vercel — no internal Docker network)
INTERNAL_API_URL=https://your-backend.railway.app
